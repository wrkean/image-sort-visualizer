[package]
name = "sort"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
image = "0.25.10"
macroquad = "0.4.16"
rand = "0.10.2"
rodio = { version = "0.22", default-features = false, features = ["playback"] }
